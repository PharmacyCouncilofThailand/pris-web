# ACCP 2026 Web Project - Improvement Roadmap

เอกสารฉบับนี้รวบรวมแผนการปรับปรุงโครงสร้างและประสิทธิภาพของโปรเจ็กต์ `pris-web` (ACCP 2026) เพื่อให้โค้ดสะอาด ดูแลรักษาง่าย และมีประสิทธิภาพสูงสุดในระยะยาว

---

## 📅 Phase 1: Cleaning & Debloating (ล้างบ้านและลดน้ำหนัก)
**เป้าหมาย:** กำจัดขยะและสิ่งที่ไม่ได้ใช้ เพื่อลดความซับซ้อนและขนาดของโปรเจ็กต์ทันที
1.  **Remove Dead Code:**
    *   ลบไฟล์ `components/sections/home1/section1.tsx` ถึง `section9.tsx` ที่ไม่ได้ใช้งาน
    *   ตรวจสอบและลบไฟล์ CSS/SCSS ที่ไม่ได้ถูก import
2.  **Dependencies Cleanup:**
    *   เลือก Library สำหรับ Slider เพียงตัวเดียว (แนะนำ `Swiper` ลบ `react-slick` และ `slick-carousel`)
    *   ถอนการติดตั้ง Package ที่ไม่ได้ใช้ใน `package.json`

## 🛠 Phase 2: Refactoring & Standardization (จัดระเบียบและสร้างมาตรฐาน)
**เป้าหมาย:** แก้ไขหนี้ทางเทคนิค (Technical Debt) โดยเฉพาะเรื่อง CSS และ Responsive
1.  **CSS Architecture Fix:**
    *   ย้าย Inline CSS (`<style>...</style>` ในไฟล์ .tsx) ทั้งหมดไปไฟล์ `.scss`
    *   ลบการใช้ `!important` ที่ไม่จำเป็น
    *   ใช้ **SCSS Mixins** (`@include respond-to`) ของโปรเจ็กต์แทนการ Hardcode media queries
2.  **Image Optimization:**
    *   เปลี่ยน tag `<img>` เป็น `<Image />` component ของ Next.js ทั้งหมด เพื่อเปิดใช้ Lazy Loading และ Auto Formatting (WebP/AVIF)
    *   เปิดกฎ ESLint `@next/next/no-img-element` กลับคืนมา
3.  **Internationalization (I18n) Check:**
    *   ตรวจสอบโค้ดเก่าที่อาจมีการเขียน logic สลับภาษาเอง ให้เปลี่ยนมาใช้ `next-intl` ทั้งหมดให้เป็นมาตรฐานเดียว

## 🚀 Phase 3: Performance & Best Practices (รีดประสิทธิภาพ)
**เป้าหมาย:** คะแนน Lighthouse เขียว และ User Experience ที่ลื่นไหล
1.  **Lighthouse Audit:**
    *   แก้ไขจุดที่คะแนนตก (LCP, CLS, FID)
    *   ตรวจสอบ Accessibility (Contrast ratio, Alt tags)
2.  **Code Splitting & Dynamic Imports:**
    *   ตรวจสอบว่า Component ใหญ่ๆ (เช่น Map, Video Modal) ถูกโหลดแบบ Dynamic หรือไม่ (ปัจจุบันทำไว้บ้างแล้ว แต่ควรตรวจสอบเพิ่ม)
3.  **SEO & Metadata:**
    *   ตรวจสอบ Meta tags ในทุกหน้าว่าเป็น Dynamic และถูกต้องตามหลัก SEO

## 🛡 Phase 4: Reliability & Testing (ความเสถียร)
**เป้าหมาย:** ลดโอกาสเกิดบั๊กเมื่อมีการแก้โค้ดในอนาคต
1.  **Type Safety:**
    *   ตรวจสอบ TypeScript types ให้เคร่งครัดขึ้น (ลดการใช้ `any`)
2.  **Basic Testing:**
    *   (Optional) เพิ่ม Unit Test สำหรับ Utility functions ที่สำคัญ
    *   (Optional) E2E Test สำหรับ Flow สำคัญ เช่น การลงทะเบียน

---

## 📝 Action Items (สิ่งที่ทำได้เลยทันที)
- [ ] ลบไฟล์ `section1-9.tsx`
- [ ] Refactor `HeroSection.tsx` ย้าย CSS ไปไฟล์ SCSS
- [ ] เปลี่ยนรูป Logo เป็น `<Image />` component
